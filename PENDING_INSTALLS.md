# Pending installs

All caught up:

- `qrcode` + `@types/qrcode` — gift QR generates locally in [`src/components/invitation/BankQr.tsx`](src/components/invitation/BankQr.tsx)
- `prettier` — `pnpm prettier`
- Fonts — Outfit + Cormorant Garamond via Fontsource in [`src/app/layout.tsx`](src/app/layout.tsx) (self-hosted; no Google fetch at compile time)

You can delete this file whenever you like.
