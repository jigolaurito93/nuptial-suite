import { PlaceholderPage } from "@/components/ui";

export default function PlannerDashboardPage() {
  return (
    <PlaceholderPage
      title="Planner dashboard"
      description="Overview for the couple: upcoming tasks, RSVP counts, and budget snapshot. Data will come from Supabase later."
    />
  );
}
